# password-generator
HTML, CSS, Javascript Vanilla

<p>Create a random password generator using HTML CSS JS.  Also used CSS and JS to have a button display both dark and light modes per the design specs.</p>
